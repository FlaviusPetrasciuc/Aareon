# SMTP (used by /api/submit-intake to email the manager + recruiter)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
RECRUITER_EMAIL=

# AI Agent (OpenAI-compatible — works with OpenAI, OpenRouter, Groq, etc.)
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o
